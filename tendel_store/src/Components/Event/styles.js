import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    eventIcons: {
        color: '#f50057',
        marginRight: "0.2rem"
    },
    iconAndtext: {
        display: "flex", 
        justifyContent: "center"
    }

}))